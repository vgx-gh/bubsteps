# Never track the live database(s) or backups - this is real data, not code.
# auth.db = shared login table (emails/password hashes). data/ = each family's
# own tracker_<user_id>.db. tracker.db is kept too for the old single-user layout.
auth.db
data/
tracker.db
backups/

static/uploads/

# Python runtime junk
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db
